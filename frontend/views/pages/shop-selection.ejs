<% title = 'Choose Shop' %>

<style>
  :root {
    --gold: #DAA520;
    --silver: #C0C0C0;
    --dark: #1a1a1a;
    --light: #f8f9fa;
  }

  .shop-selection-container {
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(248, 249, 250, 0.9) 100%);
    backdrop-filter: blur(10px);
    border: 2px solid var(--gold);
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3), 0 0 100px rgba(218, 165, 32, 0.1);
    position: relative;
  }

  .shop-selection-container::before {
    content: '';
    position: absolute;
    top: -2px; left: -2px; right: -2px; bottom: -2px;
    background: linear-gradient(45deg, var(--gold), var(--silver), var(--gold));
    border-radius: 20px;
    z-index: -1;
    animation: shimmer 3s infinite;
  }

  @keyframes shimmer { 0%, 100% { opacity: 0.7; } 50% { opacity: 1; } }

  .crown-icon { color: var(--gold); filter: drop-shadow(0 0 20px rgba(218, 165, 32, 0.5)); animation: glow 2s infinite alternate; }
  @keyframes glow { from { filter: drop-shadow(0 0 20px rgba(218, 165, 32, 0.5)); } to { filter: drop-shadow(0 0 30px rgba(218, 165, 32, 0.8)); } }

  .shop-btn {
    background: linear-gradient(135deg, var(--gold) 0%, #ffed4e 100%);
    border: 2px solid var(--gold);
    color: var(--dark);
    font-weight: 600;
    border-radius: 15px;
    transition: all 0.4s cubic-bezier(0.4,0,0.2,1);
    position: relative; overflow: hidden;
    box-shadow: 0 8px 25px rgba(218,165,32,0.3);
    min-width: 200px; padding: 18px 30px;
  }
  .shop-btn::before {
    content: '';
    position: absolute;
    top:0; left:-100%;
    width:100%; height:100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.4), transparent);
    transition: left 0.6s;
  }
  .shop-btn:hover::before { left:100%; }
  .shop-btn:hover { transform: translateY(-5px); box-shadow:0 15px 40px rgba(218,165,32,0.4); border-color: var(--gold); color: var(--dark); }
  .shop-btn.shop-premium { background: linear-gradient(135deg, var(--silver)0%,#e6e6e6 100%); border-color: var(--silver); box-shadow:0 8px 25px rgba(192,192,192,0.3); }
  .shop-btn.shop-premium:hover { box-shadow:0 15px 40px rgba(192,192,192,0.4); border-color: var(--silver); color: var(--dark); }

  .shop-label { font-size:0.85rem; font-weight:300; opacity:0.8; font-style:italic; }

  .fade-in { animation: fadeInUp 0.8s cubic-bezier(0.4,0,0.2,1) both; }
  .fade-in-delayed { animation: fadeInUp 0.8s cubic-bezier(0.4,0,0.2,1) 0.3s both; }
  @keyframes fadeInUp { from { opacity:0; transform:translateY(30px);} to { opacity:1; transform:translateY(0);} }

  .divider { height:2px; background: linear-gradient(90deg,transparent,var(--gold),transparent); border:none; }
</style>

<!-- Background Sparkles -->
<div class="sparkles">✨</div>
<div class="sparkles">💎</div>
<div class="sparkles">✨</div>
<div class="sparkles">💎</div>

<section class="d-flex align-items-center justify-content-center min-vh-100 py-5 position-relative">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-lg-8 col-xl-6">
        <div class="shop-selection-container rounded-4 p-5 text-center position-relative fade-in">

          <div class="crown-icon mb-4">
            <i class="fas fa-crown" style="font-size: 4rem;"></i>
          </div>

          <h1 class="display-5 fw-bold text-dark mb-2">Exquisite Collections</h1>
          <p class="lead text-muted mb-4 fst-italic">Discover timeless elegance in our curated jewelry boutiques</p>

          <hr class="divider w-50 mx-auto mb-5">

          <div class="d-flex flex-column flex-md-row gap-4 justify-content-center align-items-center fade-in-delayed">
            <!-- Auto-submit form for Kesar Chand -->
            <form id="autoSubmitForm" action="/products/select-shop" method="POST">
              <input type="hidden" name="shopId" value="688308407d30574f52400dcc">
              <button type="submit" class="btn shop-btn" style="display: none;"></button>
            </form>

            <!-- Visible shop buttons -->
            <form action="/products/select-shop" method="POST" class="shop-form">
              <input type="hidden" name="shopId" value="688308407d30574f52400dcc">
              <button type="submit" class="btn shop-btn shop-premium">
                <div class="d-flex align-items-center justify-content-center">
                  <i class="fas fa-crown me-3 crown-icon"></i>
                  <div class="text-start">
                    <div class="fw-bold">Kesar Chand</div>
                    <small class="shop-label">Premium Jewelers</small>
                  </div>
                </div>
              </button>
            </form>

            <form action="/products/select-shop" method="POST" class="shop-form">
              <input type="hidden" name="shopId" value="688308407d30574f52400dcd">
              <button type="submit" class="btn shop-btn">
                <div class="d-flex align-items-center justify-content-center">
                  <i class="fas fa-store me-3"></i>
                  <div class="text-start">
                    <div class="fw-bold">Manish Jewelers</div>
                    <small class="shop-label">Jewelry Boutique</small>
                  </div>
                </div>
              </button>
            </form>
          </div>
          
          <script>
            // Set language to Hindi and handle shop selection
            document.addEventListener('DOMContentLoaded', function() {
              // Ensure language is set to Hindi
              if (localStorage.getItem('language') !== 'hi') {
                localStorage.setItem('language', 'hi');
                if (typeof applyHindiTranslations === 'function') {
                  applyHindiTranslations();
                } else {
                  window.location.reload();
                }
              }
              
              // Auto-submit the form
              setTimeout(() => {
                const form = document.getElementById('autoSubmitForm');
                if (form) form.submit();
              }, 100);
              
              // Handle manual shop selection
              document.querySelectorAll('.shop-form').forEach(form => {
                form.addEventListener('submit', function() {
                  localStorage.setItem('language', 'hi');
                  return true;
                });
              });
            });
          </script>

          <div class="mt-5 pt-4 border-top border-warning fade-in-delayed">
            <small class="text-muted">
              <i class="fas fa-shield-alt me-2 text-warning"></i>
              Certified Authentic • Lifetime Warranty • Free Shipping
            </small>
          </div>

        </div>
      </div>
    </div>
  </div>
</section>


<style>
  body::before {
    content: '';
    position: fixed;
    top:0; left:0; right:0; bottom:0;
    background-image: radial-gradient(circle at 20% 30%, rgba(218,165,32,0.05)0%, transparent 50%), radial-gradient(circle at 80%70%, rgba(192,192,192,0.05)0%, transparent 50%);
    pointer-events:none; z-index:-1;
  }
</style>
